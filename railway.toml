[build]
builder = "RAILPACK"
buildCommand = "pnpm install && pnpm -F @catalogpro/api db:push && pnpm -F @catalogpro/api build"

[deploy]
startCommand = "pnpm -F @catalogpro/api db:push && pnpm -F @catalogpro/api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
